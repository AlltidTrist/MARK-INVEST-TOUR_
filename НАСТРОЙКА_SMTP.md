# Инструкция по настройке SMTP для email рассылки

## 🚀 Быстрая настройка

### 1. Создайте файл `.env`

В корневой папке проекта создайте файл `.env` (или скопируйте `.env.example`):

```bash
# Windows PowerShell
Copy-Item .env.example .env

# Linux/Mac
cp .env.example .env
```

### 2. Настройте SMTP параметры

Откройте файл `.env` и укажите ваши SMTP настройки:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш-email@gmail.com
SMTP_PASS=ваш-пароль-приложения
SITE_URL=http://localhost:3000
```

### 3. Перезапустите сервер

После изменения `.env` файла обязательно перезапустите сервер:

```bash
# Остановите сервер (Ctrl+C) и запустите заново
npm start
```

---

## 📧 Настройка для разных почтовых сервисов

### Gmail (рекомендуется для тестирования)

1. **Включите двухфакторную аутентификацию** в вашем Google аккаунте
   - Перейдите: https://myaccount.google.com/security
   - Включите "Двухэтапная аутентификация"

2. **Создайте пароль приложения**:
   - Перейдите: https://myaccount.google.com/apppasswords
   - Выберите "Почта" и "Другое (назовите приложение)"
   - Введите название: "MARK INVEST TOUR"
   - Скопируйте сгенерированный 16-значный пароль

3. **Настройте `.env`**:
   ```env
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=ваш-email@gmail.com
   SMTP_PASS=ваш-16-значный-пароль-приложения
   ```

### Mail.ru

```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=ваш-email@mail.ru
SMTP_PASS=ваш-пароль
```

**Важно:** В `server.js` для порта 465 установите `secure: true`:
```javascript
const emailTransporter = nodemailer.createTransport({
  host: process.env.SMTP_HOST,
  port: parseInt(process.env.SMTP_PORT || '587'),
  secure: process.env.SMTP_PORT === '465', // true для 465, false для других
  auth: {
    user: process.env.SMTP_USER,
    pass: process.env.SMTP_PASS
  }
});
```

### Yandex

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=ваш-email@yandex.ru
SMTP_PASS=ваш-пароль
```

### Другие SMTP сервисы

Для других провайдеров (SendGrid, Mailgun, AWS SES и т.д.) используйте их документацию для получения SMTP параметров.

---

## ✅ Проверка настройки

После настройки `.env` и перезапуска сервера проверьте подключение:

### В консоли браузера админ-панели:

```javascript
fetch('/api/test-smtp', {
    headers: {
        'Authorization': 'Bearer ' + localStorage.getItem('authToken')
    }
})
.then(r => r.json())
.then(result => {
    if (result.success) {
        console.log('✅ SMTP настроен правильно!', result);
    } else {
        console.error('❌ Ошибка:', result);
    }
});
```

### Ожидаемый успешный ответ:

```json
{
  "success": true,
  "message": "SMTP подключение успешно",
  "smtp": {
    "host": "smtp.gmail.com",
    "port": "587",
    "user": "ваш-email@gmail.com"
  }
}
```

---

## 🔧 Решение проблем

### Проблема: "SMTP не настроен"

**Решение:**
- Убедитесь, что файл `.env` существует в корне проекта
- Проверьте, что переменные `SMTP_USER` и `SMTP_PASS` заполнены
- **Перезапустите сервер** после изменения `.env`

### Проблема: "Ошибка аутентификации"

**Для Gmail:**
- Убедитесь, что используется "Пароль приложения", а не обычный пароль
- Проверьте, что двухфакторная аутентификация включена
- Убедитесь, что скопировали полный 16-значный пароль без пробелов

**Для других сервисов:**
- Проверьте правильность пароля
- Убедитесь, что SMTP включен в настройках почтового аккаунта
- Проверьте, что не требуется дополнительная авторизация

### Проблема: "Ошибка подключения к SMTP серверу"

**Решение:**
- Проверьте правильность `SMTP_HOST` и `SMTP_PORT`
- Убедитесь, что ваш интернет-провайдер не блокирует SMTP порты
- Попробуйте использовать другой порт (587 вместо 465 или наоборот)
- Проверьте файрвол на вашем компьютере/сервере

### Проблема: Письма не отправляются

**Решение:**
- Проверьте логи сервера на наличие ошибок
- Убедитесь, что в базе данных есть активные подписчики
- Проверьте спам-папку получателя
- Для Gmail проверьте лимиты отправки (500 писем/день для бесплатных аккаунтов)

---

## 🔒 Безопасность

⚠️ **Важно:** Файл `.env` содержит конфиденциальные данные (пароли, токены).

1. **Никогда не коммитьте `.env` в Git**
   - Убедитесь, что `.env` добавлен в `.gitignore`
   
2. **На продакшене используйте переменные окружения сервера**
   - Не храните `.env` файл на сервере
   - Используйте системные переменные окружения или секреты облачного провайдера

3. **Используйте разные учетные записи для тестирования и продакшена**
   - Создайте отдельный email для тестирования
   - Не используйте личный email для продакшена

---

## 📝 Примеры настройки для популярных сервисов

### SendGrid

```env
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=ваш-api-ключ-sendgrid
```

### Mailgun

```env
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=postmaster@ваш-домен.mailgun.org
SMTP_PASS=ваш-пароль-mailgun
```

### AWS SES

```env
SMTP_HOST=email-smtp.region.amazonaws.com
SMTP_PORT=587
SMTP_USER=ваш-access-key-id
SMTP_PASS=ваш-secret-access-key
```

---

**Дата создания:** 2025-01-27
**Версия:** 1.0

