# Инструкция по проверке email рассылки

## 🔍 Способы проверки работы email рассылки

### 1. Проверка настроек SMTP

Убедитесь, что в файле `.env` (или в переменных окружения) настроены SMTP параметры:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш-email@gmail.com
SMTP_PASS=ваш-пароль-приложения
SITE_URL=http://localhost:3000
```

**Важно:** Для Gmail необходимо использовать "Пароль приложения", а не обычный пароль.

### 2. Проверка списка подписчиков в базе данных

#### Через SQLite (если используется локальная БД):
```bash
sqlite3 travel.db
SELECT * FROM tour_subscriptions WHERE status = 'active';
```

#### Через API (требует авторизации):
```bash
# Сначала получите токен через /api/admin/login
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:3000/api/subscriptions
```

#### Через админ-панель:
1. Откройте `/admin/index.html`
2. Войдите в систему
3. Откройте консоль браузера (F12)
4. Выполните:
```javascript
fetch('/api/subscriptions', {
    headers: {
        'Authorization': 'Bearer ' + localStorage.getItem('authToken')
    }
})
.then(r => r.json())
.then(data => console.log('Подписчики:', data));
```

### 3. Проверка логов сервера

При создании нового тура сервер выводит в консоль информацию об отправке:

**Успешная отправка:**
```
Отправлено 5 уведомлений о новом туре 123
```

**Ошибки:**
```
Ошибка отправки email на user@example.com: [описание ошибки]
Email не настроен. Пропускаем отправку уведомлений.
```

**Проверка логов:**
- Откройте терминал, где запущен сервер (`node server.js`)
- Создайте новый тур через админ-панель
- Следите за выводом в консоль

### 4. Тестовая отправка email

#### Через тестовый endpoint (после создания тура):

**В админ-панели откройте консоль браузера (F12) и выполните:**

```javascript
// Получить список туров и последний тур
fetch('/api/tours', {
    headers: {
        'Authorization': 'Bearer ' + localStorage.getItem('authToken')
    }
})
.then(r => r.json())
.then(tours => {
    const lastTour = tours[tours.length - 1];
    console.log('📋 Тестируем отправку для тура:', lastTour);
    
    // Запустить тестовую отправку
    fetch(`/api/test-email/${lastTour.id}`, {
        method: 'POST',
        headers: {
            'Authorization': 'Bearer ' + localStorage.getItem('authToken')
        }
    })
    .then(r => r.json())
    .then(result => {
        if (result.success) {
            console.log('✅ Тестовая отправка успешна!', result);
            console.log(`📧 Отправлено ${result.subscribers} писем на адреса:`, result.emails);
        } else {
            console.error('❌ Ошибка отправки:', result);
        }
    });
});
```

**Через curl (если есть токен):**
```bash
# Получить ID последнего тура (требуется jq)
TOUR_ID=$(curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:3000/api/tours | jq '.[-1].id')

# Запустить тестовую отправку
curl -X POST \
  -H "Authorization: Bearer YOUR_TOKEN" \
  http://localhost:3000/api/test-email/$TOUR_ID
```

### 4.1. Проверка подключения SMTP

**В консоли браузера админ-панели:**

```javascript
fetch('/api/test-smtp', {
    headers: {
        'Authorization': 'Bearer ' + localStorage.getItem('authToken')
    }
})
.then(r => r.json())
.then(result => {
    if (result.success) {
        console.log('✅ SMTP подключение работает!', result);
        console.log('📧 Настройки SMTP:', result.smtp);
    } else {
        console.error('❌ Ошибка SMTP:', result);
    }
});
```

**Через curl:**
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:3000/api/test-smtp
```

### 5. Проверка подписки через форму на сайте

1. Откройте главную страницу сайта
2. Прокрутите до секции "Узнавайте первыми о новых турах"
3. Введите тестовый email
4. Нажмите "Подписаться"
5. Проверьте ответ в консоли браузера (должно быть `{message: "Подписка оформлена"}`)
6. Проверьте, появился ли email в базе данных (способ 2)

### 6. Проверка отправки при создании тура

1. Откройте админ-панель (`/admin/index.html`)
2. Создайте новый тестовый тур
3. Сразу после создания проверьте:
   - **Логи сервера** - должны появиться сообщения об отправке
   - **Почтовый ящик** подписчика - должно прийти письмо
   - **Спам-папку** - иногда письма попадают туда

### 7. Тестовые API endpoints

В сервер добавлены два тестовых endpoint:

1. **`POST /api/test-email/:tourId`** - Тестовая отправка email для конкретного тура
   - Требует авторизации
   - Отправляет письма всем активным подписчикам
   - Возвращает детальный отчет об отправке

2. **`GET /api/test-smtp`** - Проверка подключения к SMTP серверу
   - Требует авторизации
   - Проверяет настройки и подключение
   - Возвращает статус подключения

**Использование:** См. разделы 4 и 4.1 выше

### 8. Типичные проблемы и решения

#### Проблема: "Email не настроен"
**Решение:** Проверьте наличие `SMTP_USER` и `SMTP_PASS` в `.env`

#### Проблема: "Ошибка аутентификации"
**Решение:** 
- Для Gmail используйте "Пароль приложения" вместо обычного пароля
- Включите "Небезопасные приложения" в настройках Google аккаунта (устарело)
- Используйте OAuth2 для Gmail (рекомендуется)

#### Проблема: Письма не приходят
**Решение:**
- Проверьте спам-папку
- Убедитесь, что email подписчика указан правильно
- Проверьте логи сервера на наличие ошибок
- Проверьте лимиты SMTP сервера (количество писем в час/день)

#### Проблема: Письма приходят не всем
**Решение:**
- Проверьте статус подписчика (`status = 'active'`)
- Проверьте логи на наличие ошибок для конкретных email
- Убедитесь, что подписчики не отписались

### 9. Структура email уведомления

Email содержит:
- Заголовок: "Новый тур: [Название тура]"
- Отправитель: "MARK INVEST TOUR" <ваш-email>
- HTML версия с изображением и форматированием
- Текстовая версия для почтовых клиентов без HTML
- Ссылку на тур
- Ссылку для отписки

### 10. Мониторинг рассылки

Для постоянного мониторинга рекомендуется:
1. Настроить логирование в файл
2. Создать дашборд для отслеживания статистики отправки
3. Настроить уведомления об ошибках отправки

---

## 📝 Быстрая проверка (чек-лист)

- [ ] Настроены SMTP параметры в `.env`
- [ ] Есть активные подписчики в базе данных
- [ ] Сервер запущен и логи видны
- [ ] Тестовая подписка создана через форму
- [ ] Создан тестовый тур через админ-панель
- [ ] Проверены логи сервера после создания тура
- [ ] Проверен почтовый ящик подписчика (и спам)
- [ ] Проверена функция отписки от рассылки

---

## 🧪 Тестовый сценарий

1. Подпишитесь на рассылку со своего email через форму на сайте
2. Создайте тестовый тур через админ-панель
3. Проверьте почтовый ящик через 1-2 минуты
4. Откройте письмо и проверьте:
   - Корректность информации о туре
   - Работоспособность ссылок
   - Отображение изображения
5. Проверьте ссылку отписки от рассылки

---

**Дата создания:** 2025-01-27
**Версия:** 1.0

