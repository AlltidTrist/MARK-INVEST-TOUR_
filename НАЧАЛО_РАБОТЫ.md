# 🚀 Начало работы с админ-панелью

## Шаг 1: Установка Node.js

Если у вас не установлен Node.js, выполните следующие шаги:

1. **Скачайте Node.js:**
   - Перейдите на https://nodejs.org/
   - Скачайте LTS версию (рекомендуется)
   - Установите, следуя инструкциям установщика

2. **Проверьте установку:**
   Откройте новое окно PowerShell или командной строки и выполните:
   ```powershell
   node --version
   npm --version
   ```
   Должны отобразиться версии Node.js и npm.

## Шаг 2: Установка зависимостей проекта

Откройте PowerShell в папке проекта и выполните:

```powershell
npm install
```

Это установит все необходимые библиотеки (Express, SQLite, и др.)

## Шаг 3: Создание файла .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```
PORT=3000
JWT_SECRET=neverend-travel-secret-key-2024
```

**Или выполните в PowerShell:**
```powershell
@"
PORT=3000
JWT_SECRET=neverend-travel-secret-key-2024
"@ | Out-File -FilePath .env -Encoding utf8
```

## Шаг 4: Запуск сервера

Выполните команду:

```powershell
npm start
```

Или для режима разработки (с автоперезагрузкой):

```powershell
npm run dev
```

Вы должны увидеть сообщение:
```
Сервер запущен на http://localhost:3000
Админ-панель: http://localhost:3000/admin
```

## Шаг 5: Вход в админ-панель

1. Откройте браузер
2. Перейдите по адресу: **http://localhost:3000/admin**
3. Введите данные для входа:
   - **Логин:** `admin`
   - **Пароль:** `admin123`

## Шаг 6: Первые действия в админ-панели

### Добавление первого тура:

1. Нажмите кнопку **"+ Добавить тур"**
2. Заполните форму:
   - Название тура (обязательно)
   - Описание
   - Цена
   - Местоположение
   - Длительность
   - Загрузите изображение
3. Нажмите **"Сохранить"**

### Просмотр заявок:

1. Перейдите в раздел **"Заявки"**
2. Здесь будут отображаться все заявки с формы обратной связи
3. Вы можете фильтровать их по статусу (новые/обработанные)
4. Нажмите **"Отметить как обработанную"** для обработки заявки

### Статистика:

В разделе **"Статистика"** вы увидите:
- Количество активных туров
- Общее количество туров
- Количество новых заявок
- Общее количество заявок

## ⚠️ Важные замечания

1. **Смените пароль администратора** после первого входа!
2. База данных `travel.db` создается автоматически при первом запуске
3. Все изображения туров сохраняются в папку `assets/images/`
4. Регулярно делайте резервные копии файла `travel.db`

## 🔧 Решение проблем

### Проблема: "npm не является внутренней или внешней командой"
**Решение:** Установите Node.js с официального сайта nodejs.org

### Проблема: Порт 3000 уже занят
**Решение:** Измените PORT в файле `.env` на другой (например, 3001)

### Проблема: Не могу войти в админ-панель
**Решение:** 
- Убедитесь, что сервер запущен
- Проверьте логин: `admin` и пароль: `admin123`
- Очистите кэш браузера

### Проблема: Туры не отображаются на сайте
**Решение:**
- Убедитесь, что туры имеют статус "Активный"
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что сервер запущен

## 📞 Дополнительная помощь

Если возникли проблемы, проверьте:
- Файл `INSTALL.md` - подробная документация
- Файл `README_ADMIN.md` - описание функционала
- Консоль сервера на наличие ошибок

